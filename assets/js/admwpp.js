/**
 * Main Administrate Template JS file
 */
(function ($) {
    $(document).ready(function () {

      // Selectric
      if(jQuery().selectric) {
         $('.admwpp-custom-select').selectric();
      }

    });
  }(jQuery));
